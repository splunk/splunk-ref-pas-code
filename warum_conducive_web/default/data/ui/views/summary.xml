<form script="summary.js">
	<label>Summary</label>
	<fieldset autoRun="true" submitButton="false">
        <!-- Has the ID "timepicker" by default. -->
	    <input type="time" searchWhenChanged="true">
	      <default>
	        <earliestTime>-24h</earliestTime>
	        <latestTime>now</latestTime>
	      </default>
	    </input>
  	</fieldset>
  	<row>
  		<single id="policy_single">
  			<title>Policy Violations</title>
  			<searchString>| pivot ri_pas_datamodel Policy_Violation count(Policy_Violation) AS count FILTER command isNotNull $filter$ $exclude$ ROWSUMMARY 0 COLSUMMARY 0 NUMCOLS 0 SHOWOTHER 1</searchString>
  			<option name="beforeLabel">Policy Violations:</option>
  		</single>
  	</row>
	<row>
		<chart id="trend_chart">
			<title>Trend</title>
			<searchString>| pivot ri_pas_datamodel Root_Event count(Root_Event) AS count SPLITROW _time AS _time PERIOD auto SPLITCOL command FILTER command isNotNull $filter$ $exclude$ SORT 0 _time ROWSUMMARY 0 COLSUMMARY 0 NUMCOLS 10 SHOWOTHER 0</searchString>
			<option name="charting.chart">column</option>
            <option name="charting.chart.stackMode">stacked</option>
            <option name="drilldown">none</option>
		</chart>
	</row>
	<row>
		<table id="user_table">
			<title>Top Users</title>
			<searchString>| pivot ri_pas_datamodel Root_Event count(Root_Event) AS Count SPLITROW _time AS _time PERIOD auto SPLITROW user AS user SPLITROW command AS command SPLITROW object AS object FILTER command isNotNull $filter$ $exclude$ ROWSUMMARY 0 COLSUMMARY 0 NUMCOLS 0 SHOWOTHER 1 | stats count by user | sort - count</searchString>
		</table>
		<table id="document_table">
			<title>Top Documents</title>
			<searchString>| pivot ri_pas_datamodel Root_Event count(Root_Event) AS Count SPLITROW _time AS _time PERIOD auto SPLITROW user AS user SPLITROW command AS command SPLITROW object AS object FILTER command isNotNull $filter$ $exclude$ ROWSUMMARY 0 COLSUMMARY 0 NUMCOLS 0 SHOWOTHER 1 | stats count by object | sort - count</searchString>
		</table>
	</row>
</form>